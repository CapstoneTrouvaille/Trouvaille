import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProfilePictureScreen = () => {
  return (
    <View>
      <Text>ProfilePictureScreen</Text>
    </View>
  );
};

export default ProfilePictureScreen;

const styles = StyleSheet.create({});
